{
  "name": "gpcbtinewweb-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm --filter ./apps/client build",
    "start": "pnpm --filter ./apps/server start"
  },
  "devDependencies": {
    "prisma": "^7.1.0"
  },
  "dependencies": {
    "@prisma/client": "^7.1.0"
  }
}
